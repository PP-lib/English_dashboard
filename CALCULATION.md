# 計算条件 仕様書（算出ルールの定義）

オンライン英会話（1コマ25分）の成績指標の算出条件を明示する。
実装は `analyze.py` / `audio_speaking_time.py` / `build_dashboard.py`。

最終更新: 2026-06-25

---

## 前提・対象データ

- **授業時間**: 1コマ 25分。
- **入力**: 授業ごとに「文字起こし(.txt)」と「音声(.mp3)」をペアで保存する。
- **録音の前提**: 録音には **生徒（受講者本人）の声のみ** が入っている（オンライン授業で先生の声は未録音）。
  したがって音声中の発話部分はすべて生徒の発話とみなす。
- **文字起こしの前提**: 文字起こしも生徒の発話を対象とする。

---

## 指標の定義

### 1. 総発話数（total_utterances）
- 文字起こしの **発話セグメント数**。
- 定義: 空行で区切られた **非空の行を1発話** として数える。
- 実装: `analyze.segments()`。

### 2. 総単語数（total_words）※フィラー除外後
- 英単語トークンの個数（**フィラーを除外**）。
- トークン定義（正規表現）: `[a-z]+(?:'[a-z]+)?`
  - すべて小文字化してから抽出。
  - アポストロフィを含む語（`don't`, `i'd`, `it's` など）は1語として数える。
  - 数字・記号は単語に含めない。
- **除外するフィラー（言いよどみ・非語彙的なつなぎ音）**:
  `ah, ahh, uh, uhh, um, umm, hmm, hmmm, hm, mm, mmm, mhm, eh, er, err, oh, huh`
  - 意味を持つ語（`yeah, yes, no, right, so, like, well, okay` 等）は **除外しない**。
  - 一覧は `analyze.FILLERS`。

### 3. ユニーク単語数（unique_words）
- フィラー除外後トークンの **異なり語数**（小文字化したサーフェスフォームの集合サイズ）。
- 語形変化（go / went、apple / apples 等）は別語として数える（レンマ化は行わない）。

### 4. 生徒の発話時間（speaking_minutes）
- WPMの分母に使う「生徒が実際に声を出していた時間（分）」。
- 音声から次の手順で推定（`audio_speaking_time.py`、ffmpeg不要）:
  1. mp3を **16kHz・モノラル・float32** にデコード（`miniaudio`）。
  2. **フレーム分割**: 窓25ms（400サンプル）/ ホップ10ms（160サンプル）、ハミング窓。
  3. **VAD（発話判定）**: フレームの対数エネルギー `log(Σx²)` を求め、
     適応しきい値 `threshold = P15 + 0.30 × (P95 − P15)`（P=パーセンタイル）超を発話とする。
  4. **平滑化**: 150ms以下の無音は発話で埋め、200ms未満の孤立発話は除去。
  5. **発話フレームの合計時間 = 生徒の発話時間**（無音・先生のターンは自動的に除外）。
- 推定値は `data/lessons.csv` に **キャッシュ** する。`speaking_minutes` を手で書き換えれば
  手動補正でき、`source` を `manual` にすると自動再計算されない。
- 音声が無い日は、暫定的に授業長 **25分** を分母に使う（`wpm_lesson` と同じ扱い）。
- 補足: VADは沈黙（考えている間の無音）も除くため、WPMは「声を出している時間あたりの発話速度」を表す。

### 5. WPM（words per minute）
- **主指標** `wpm` = `total_words ÷ speaking_minutes`（生徒の発話時間あたり）。
- **参考** `wpm_lesson` = `total_words ÷ 25`（授業25分あたりの発話量）。

### 6. TTR（語彙多様性 / type-token ratio）
- `ttr` = `unique_words ÷ total_words`。
- 値が高いほど多様な語を使っている。
  注: 発話量が増えるほど低くなりやすい指標である点に留意。

---

## 端数・丸め

- WPM・WPM(授業)・speaking_minutes: 小数第1〜2位で四捨五入（CSVは2桁）。
- TTR: 小数第3位で四捨五入。

---

## ファイルと役割

| ファイル | 役割 |
|---|---|
| `analyze.py` | 文字指標（発話数・単語数・ユニーク・TTR、フィラー除外） |
| `audio_speaking_time.py` | 音声から生徒の発話時間を推定（VAD） |
| `build_dashboard.py` | 全レッスン集計 → `data/results.csv` と `index.html` を生成 |
| `data/transcripts/` | 文字起こし（`YYYY-MM-DD_タイトル.txt`） |
| `data/audio/` | 音声（同名 `.mp3`）※Git管理外 |
| `data/lessons.csv` | 発話時間のキャッシュ（手動補正可） |
| `data/results.csv` | 集計結果 |
| `index.html` | ダッシュボード（GitHub Pagesで公開） |

---

## 変更履歴

- 2026-06-25: 初版。フィラー除外、生徒の発話時間でのWPM算出（単一話者録音前提）を定義。
